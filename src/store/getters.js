export const collections = (state) => state.collections;
export const page = (state) => state.page;
export const totalPages = (state) => state.totalPages;
export const collection = (state) => state.collection;
export const glasses = (state) => state.glasses;
export const filterOptions = (state) => state.filterOptions;